<%= @ride_request.first_name %>, 

Thank you for your interest in the Devil Dog Living History Flight Experience.

The following information was received.

<%= @ride_request.first_name %> <%= @ride_request.last_name %>
<%= @ride_request.email %>
<%= @ride_request.city %>, <%= @ride_request.state %>
Phone:  <%= @ride_request.phone %>


<% if !@ride_request.events.blank? %>
Indicated interest at the following events:
<% @ride_request.events.each do |event| %>
<%= event.title %> <% if event.start.strftime("%b %d") == event.end.strftime("%b %d") %>
<%= event.start.strftime("%b. %d, %Y") %>
<% else %>
<% if event.start.strftime("%b") == event.end.strftime("%b") %>
<%= event.start.strftime("%b. %d") %>-<%= event.end.strftime("%d, %Y") %>
<% else %>
<%= event.start.strftime("%b. %d") %>-<%= event.end.strftime("%b. %d, %Y") %>
<% end %>
<% end %>
<% end %>
<% end %>

Living History Flight Experience PrePay is <%= number_to_currency(@price.value) %> per seat, if you would like to prepay for your ride please use this link. (Flights are <%= number_to_currency(@price.alt_value) %> per seat during the event)

<%= "http://www.devildogsquadron.com" + px_path %>

If you have any questions, you can email us at info@devildogsquadron.com or call 512-869-1759.

We look forward to flying with you soon!

Best regards,

Devil Dog Squadron
