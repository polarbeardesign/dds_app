<% current_event_type = "" %>
<% airshows = "" %>
<% if signed_in? %>
  <% airshows = @air_shows_members %>
<% else %>
  <% airshows = @air_shows_public %>
<% end %>

<% content_for :sidebar do %>
  <%= render "sidebars/newsletter_signup" %>
<% end %>

<h1>Devil Dog Calendar:</h1>

<table width="500" class="table_border">
  <tr>
    <th>Date</th>
    <th>Title</th>
  </tr>

<% @meetings.each do |event| %>
<% if current_event_type != event.event_type.title %>
  <tr>
    <td colspan="2"><strong><em><%= event.event_type.title %></em></strong></td>
  </tr>
<% end %>
  <tr class="<%= cycle('odd_row', 'even_row') %>">
    <td><%= event.start.strftime("%b %d, %Y") %></td>
    <td><%= link_to event.title, event %></td>
  </tr>
<% current_event_type = event.event_type.title %>
<% end %>

<% airshows.each do |event| %>
<% if current_event_type != event.event_type.title %>
  <tr>
    <td colspan="2"><strong><em><%= event.event_type.title %></em></strong></td>
  </tr>
<% end %>
  <tr class="<%= cycle('odd_row', 'even_row') %>">
    <td><%= event.start.strftime("%b %d, %Y") %></td>
    <td><%= link_to event.title, event %><% if event.event_statuses_id == 2 %> <span class="error" style="display: inline;"><strong>--Tentative</strong></span> <% end %></td>
  </tr>
<% current_event_type = event.event_type.title %>
<% end %>

<% @special.each do |event| %>
<% if current_event_type != event.event_type.title %>
  <tr>
    <td colspan="2"><strong><em><%= event.event_type.title %></em></strong></td>
  </tr>
<% end %>
  <tr class="<%= cycle('odd_row', 'even_row') %>">
    <td><%= event.start.strftime("%b %d, %Y") %></td>
    <td><%= link_to event.title, event %></td>
  </tr>
<% current_event_type = event.event_type.title %>
<% end %>

<% @maint.each do |event| %>
<% if current_event_type != event.event_type.title %>
  <tr>
    <td colspan="2"><strong><em><%= event.event_type.title %></em></strong></td>
  </tr>
<% end %>
  <tr class="<%= cycle('odd_row', 'even_row') %>">
    <td><%= event.start.strftime("%b %d, %Y") %></td>
    <td><%= link_to event.title, event %></td>
  </tr>
<% current_event_type = event.event_type.title %>
<% end %>


</table>

<br />

<% if signed_in?%>
    <% if current_user.roles.any? {|role| role.name == "superuser" or role.name == "admin" } %>
       <%= link_to 'New Event', new_event_path %>
    <% end %>
<% end %>


