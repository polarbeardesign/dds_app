<%= form_for(@gallery_category) do |f| %>
  <% if @gallery_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gallery_category.errors.count, "error") %> prohibited this gallery_category from being saved:</h2>

      <ul>
      <% @gallery_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :category_name %><br />
    <%= f.text_field :category_name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
