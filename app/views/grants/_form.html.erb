<%= form_for(@grant) do |f| %>
  <% if @grant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grant.errors.count, "error") %> prohibited this grant from being saved:</h2>

      <ul>
      <% @grant.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="indent_row">
  <div class="cell">
  <div class="field">
    <%= f.label :right_id %><br />
    <%= f.collection_select :right_id, Right.ordered.ordered_ops.all, :id, :oper_resource, options = {:order => 'resource ASC', :prompt => true}, html_options = {:class => "styled-select"} %>
  </div>
  <div class="field">
    <%= f.label :role_id %><br />
    <%# f.text_field :role_id %>
    <%= f.collection_select :role_id, Role.all, :id, :name, options = {:order => 'resource ASC', :prompt => true}, html_options = {:class => "styled-select"} %>

  </div>
  </div>

</div>
  
  
<div class="clearer"></div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
