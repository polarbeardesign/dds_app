<h1>Editing Safety Submission</h1>

<%= render 'form' %>

<%= link_to 'Show', @safety_item %> |
<%= link_to 'Back', safety_items_path %>

<% if current_user.roles.any? {|role| role.name == "safety_officer" or role.name == "admin"} %>
  <%= render 'versions', :safety_item => @safety_item %>
<% end %>